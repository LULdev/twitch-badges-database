name: Stats sync (potat)

# 15-minute owner-stats / rarity / status-sync cadence. The Vercel Hobby plan
# only allows 2 daily cron jobs, so GitHub Actions carries the high-frequency
# schedule and calls the same authenticated endpoint.
on:
  schedule:
    # Offset minutes (never :00/:30) so the GH run cannot collide with the
    # Vercel crons at 06:00/06:30 — two concurrent potat runs doubled the
    # badge_stats time-series points and wrote duplicate changelog rows.
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch: {}

concurrency:
  group: potat-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Trigger potat stats sync
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          set -euo pipefail
          status="$(curl -s -o response.json -w "%{http_code}" \
            --max-time 240 \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "https://twitch-badges-database.vercel.app/api/cron/potat")"
          echo "HTTP ${status}"
          cat response.json
          echo
          test "$status" = "200"
